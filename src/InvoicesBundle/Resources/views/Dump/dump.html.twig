{% extends '@Invoices/Default/two_columns.html.twig' %}



	{% block col1 %}

		{% for pair in db %}
			Invoice:

			{{dump(pair["invoice_entry"])}}
		{% endfor %}

	{% endblock %}

	{% block col2 %}
	
		{% for pair in db %}
			Related invoice data:

			{{dump(pair["invoiceData_entry"])}}
		{% endfor %}

	{% endblock %}




{# {{dump(db["invoices"])}}

Invoices data:

{{dump(db["invoicesData"])}} #}